export function clamp(n){return Math.max(0,Math.min(10,Number.isFinite(n)?n:0));} export function step(s){let plant=clamp(Math.round((s.light+s.water)/2));let herb=s.food?clamp(Math.round((plant+s.water)/3)):0;let pred=s.herbivore?clamp(Math.round((herb+s.water)/3)):0;return {...s,plant,herb,pred,time:s.time+1};} export function viable(s){return s.producer&&s.herbivore&&s.predator&&s.light>0&&s.water>0;}
