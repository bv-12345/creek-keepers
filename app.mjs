import {species,scenarios} from './content.mjs';import {step,viable} from './model.mjs';let tab='habitat',s={producer:'Cattails',herbivore:'Duck',predator:'Heron',light:8,water:8,food:true,time:0,plant:0,herb:0,pred:0};const panel=document.querySelector('#panel');const button=x=>'<button class="choice" data-v="'+x+'">'+x+'</button>';function render(){let h='';if(tab==='habitat')h='<h2>Choose a creek team</h2><p>Producers make food. Herbivores eat plants. Predators eat animals. Arrows show food energy moving to the eater.</p><h3>Producers</h3>'+species.producers.map(button).join('')+'<h3>Herbivores</h3>'+species.herbivores.map(button).join('')+'<h3>Predator</h3>'+species.predators.map(button).join('')+'<div class="web">'+s.producer+' → '+s.herbivore+' → '+s.predator+'</div><button id="check">Check my habitat</button><p id="feedback"></p>';if(tab==='predict')h='<h2>Predict and experiment</h2><p>Pick a starting scenario, then predict what happens after one model step.</p>'+scenarios.map((x,i)=>'<button class="scenario" data-i="'+i+'">'+x.name+': light '+x.light+', water '+x.water+'</button>').join('')+'<fieldset><legend>What will plants do?</legend><label><input type=radio name=p value=grow checked> grow</label><label><input type=radio name=p value=shrink> shrink</label></fieldset><button id="step">Advance one day</button><div id="readout"></div>';if(tab==='restore')h='<h2>Restore the dry creek</h2><p>A new creek has plants but no food chain. Repair it by choosing water and a matching team.</p><label>Water <input id="water" type="range" min="0" max="10" value="'+s.water+'"></label><p>Water model value: <output id="wv">'+s.water+'</output>/10</p>'+species.herbivores.map(button).join('')+'<button id="restorecheck">Test the repair</button><p id="restorefeedback"></p>';panel.innerHTML=h;wire()}function wire(){document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>{let v=b.dataset.v;if(species.producers.includes(v))s.producer=v;else s.herbivore=v;render()});document.querySelector('#check')?.addEventListener('click',()=>document.querySelector('#feedback').textContent=viable(s)?'Healthy model: plants feed the duck, and the duck feeds the heron.':'Add resources and connected organisms.');document.querySelectorAll('.scenario').forEach(b=>b.onclick=()=>{let x=scenarios[b.dataset.i];s={...s,...x};render()});document.querySelector('#step')?.addEventListener('click',()=>{s=step({...s,food:s.plant>0});document.querySelector('#readout').textContent='Day '+s.time+': plants '+s.plant+'/10, herbivores '+s.herb+'/10, predators '+s.pred+'/10. '+(s.plant<5?'Less water or light limited plant growth.':'Plants had enough model resources.')});document.querySelector('#water')?.addEventListener('input',e=>{s.water=+e.target.value;document.querySelector('#wv').textContent=s.water});document.querySelector('#restorecheck')?.addEventListener('click',()=>document.querySelector('#restorefeedback').textContent=s.water>0?'Repair works in this model: water supports plants, which support the herbivore.':'Try adding water and a food source.')}document.querySelector('#start').onclick=()=>{document.querySelector('#app').hidden=false;document.querySelector('#start').hidden=true;document.querySelector('#reset').hidden=false;render()};document.querySelector('#reset').onclick=()=>location.reload();document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});
